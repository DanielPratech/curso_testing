/* const { suma } = require("../sum");
const expect = require('chai').expect;

describe('Se validaran las sumas', () => {
    describe("REquerida en la HU XXXX", () => {
        it("se espera un retorno de 0", () => {
            expect(suma(0, 0)).to.equal(0);
        });
    });
});
 */